{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open Site (Chrome)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:8080/download.html",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Serve Site"
    }
  ]
}
